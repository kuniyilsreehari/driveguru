{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the GeoTrack Pro application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user.",
          "format": "uuid"
        },
        "companyId": {
          "type": "string",
          "description": "Reference to the Company the user belongs to. (Relationship: Company 1:N User)"
        },
        "companyName": {
          "type": "string",
          "description": "The name of the company the user belongs to, if applicable."
        },
        "firstName": {
          "type": "string",
          "description": "The user's first name."
        },
        "lastName": {
          "type": "string",
          "description": "The user's last name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "role": {
          "type": "string",
          "description": "The user's role within the application.",
          "enum": [
            "Super Admin",
            "Freelancer",
            "Company",
            "Authorized Pro"
          ]
        },
        "location": {
          "type": "string",
          "description": "The expert's physical location or service area."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The user's mobile number, including country code."
        },
        "category": {
          "type": "string",
          "description": "The service category the expert belongs to."
        }
      },
      "required": [
        "id",
        "firstName",
        "lastName",
        "email",
        "role"
      ]
    },
    "Company": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Company",
      "type": "object",
      "description": "Represents a company using the GeoTrack Pro application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the company.",
          "format": "uuid"
        },
        "name": {
          "type": "string",
          "description": "The name of the company."
        },
        "address": {
          "type": "string",
          "description": "The address of the company."
        },
        "contactEmail": {
          "type": "string",
          "description": "The contact email address for the company.",
          "format": "email"
        }
      },
      "required": [
        "id",
        "name",
        "address",
        "contactEmail"
      ]
    },
    "LocationData": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "LocationData",
      "type": "object",
      "description": "Represents location data recorded for a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the location data entry.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User the location data belongs to. (Relationship: User 1:N LocationData)"
        },
        "latitude": {
          "type": "number",
          "description": "The latitude coordinate."
        },
        "longitude": {
          "type": "number",
          "description": "The longitude coordinate."
        },
        "timestamp": {
          "type": "string",
          "description": "The timestamp of when the location was recorded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "latitude",
        "longitude",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Includes user's companyId for association with a company.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/companies/{companyId}",
        "definition": {
          "entityName": "Company",
          "schema": {
            "$ref": "#/backend/entities/Company"
          },
          "description": "Stores company profiles.",
          "params": [
            {
              "name": "companyId",
              "description": "The unique identifier of the company."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/location_data/{locationDataId}",
        "definition": {
          "entityName": "LocationData",
          "schema": {
            "$ref": "#/backend/entities/LocationData"
          },
          "description": "Stores location data for each user.  Path-based ownership ensures only the user can access their own location data.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "locationDataId",
              "description": "The unique identifier of the location data entry."
            }
          ]
        }
      },
      {
        "path": "/roles_super_admin/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Collection used to assign super admin roles. The existence of a document indicates super admin privileges. This enables secure `list` operations.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support GeoTrack Pro's core features while adhering to the principles of Authorization Independence, Structural Segregation, and Access Modeling. User data, company information, and location data are stored in separate collections to maintain clarity and enforce consistent security rules. Crucially, authorization data is denormalized to avoid `get()` calls in security rules and ensure atomic operations.\n\n1.  **Authorization Independence:** A separate `/roles_super_admin/{userId}` collection determines super admin privileges based on document existence rather than user document content. This simplifies security rules and avoids hierarchical dependencies.\n\n2.  **Structural Segregation:**  User data, company data, and location data are stored in separate collections, each with distinct security requirements. This segregation simplifies security rule management and prevents unintended access.\n\n3.  **Access Modeling:**\n    *   User-owned location data is structured under `/users/{userId}/location_data/{locationDataId}`. This allows for path-based security rules, ensuring that only the user can access their own location data.\n    *   Companies are stored in a top-level `/companies` collection.\n    *   The super admin role is managed through the `/roles_super_admin/{userId}` collection, where the existence of a document for a user ID grants super admin privileges.\n\nThis structure supports the required QAPs (Rules are not Filters) by using structural segregation and existence checks for authorization. Listing location data is secured by the path-based ownership under each user document."
  }
}
